import { ResaltarNombreDirective } from './resaltar-nombre.directive';

describe('ResaltarNombreDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarNombreDirective();
    expect(directive).toBeTruthy();
  });
});
