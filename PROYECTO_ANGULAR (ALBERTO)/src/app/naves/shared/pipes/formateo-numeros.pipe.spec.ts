import { FormateoNumerosPipe } from './formateo-numeros.pipe';

describe('FormateoNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateoNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
