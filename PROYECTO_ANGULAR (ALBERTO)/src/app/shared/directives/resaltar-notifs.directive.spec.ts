import { ResaltarNotifsDirective } from './resaltar-notifs.directive';

describe('ResaltarNotifsDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarNotifsDirective();
    expect(directive).toBeTruthy();
  });
});
